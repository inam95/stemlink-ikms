# ============================================
# IKMS Backend - Environment Configuration
# ============================================
# Copy this file to .env and fill in your values
# Required variables must be set for the system to work

# ============================================
# OpenAI Configuration (REQUIRED)
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your_openai_api_key_here

# Model used for all agents (Planning, Retrieval, Summarization, Verification)
# Default: gpt-4o-mini (recommended for cost-effectiveness)
# Alternatives: gpt-4o, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL_NAME=gpt-4o-mini

# Model used for generating document embeddings
# Default: text-embedding-3-small (1536 dimensions)
# Alternatives: text-embedding-3-large, text-embedding-ada-002
OPENAI_EMBEDDINGS_MODEL_NAME=text-embedding-3-small

# ============================================
# Pinecone Configuration (REQUIRED)
# ============================================
# Get your API key from: https://app.pinecone.io/
PINECONE_API_KEY=your_pinecone_api_key_here

# Name of your Pinecone index
# Must match the index you created in Pinecone console
# Index should be configured with:
#   - Dimensions: 1536 (for text-embedding-3-small)
#   - Metric: Cosine similarity
PINECONE_INDEX_NAME=your_index_name_here

# ============================================
# Retrieval Configuration (OPTIONAL)
# ============================================
# Number of document chunks to retrieve per sub-question
# Higher values = more context but slower and more expensive
# Default: 4 (recommended for balanced performance)
RETRIEVAL_K=4

# ============================================
# Notes
# ============================================
# - Do NOT commit the .env file with real credentials
# - Add .env to .gitignore (already done in this project)
# - For production, use environment variables or secrets management
# - Restart the server after changing environment variables
